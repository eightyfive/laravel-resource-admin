{% if options.help_block.text and not options.is_child %}
    <{{ options.help_block.tag }} {{ options.help_block.helpBlockAttrs|raw }}>
        {{ options.help_block.text }}
    </{{ options.help_block.tag }}>
{% endif %}
